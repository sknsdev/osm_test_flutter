name: donenergo_osm_test
description: "A new Flutter project."
publish_to: 'none'
version: 1.0.0+1

environment:
  sdk: '>=3.4.0 <4.0.0'


dependencies:
  flutter:
    sdk: flutter
  flutter_map: 7.0.1
  flutter_bloc: ^8.0.0
  camera: ^0.10.0
  location: ^4.3.0
  equatable: ^2.0.3
  geolocator: ^12.0.0
  cupertino_icons: ^1.0.6
  latlong2: ^0.9.1

dev_dependencies:
  flutter_test:
    sdk: flutter
  flutter_lints: ^3.0.0

flutter:
  uses-material-design: true

